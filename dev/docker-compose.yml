version: '2'
services:
  postgres:
    image: postgres:9.6
    container_name: 'slick-postgres'
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_PASSWORD=test
      - POSTGRES_USER=test